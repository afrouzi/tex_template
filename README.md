Move to the parent directory where you want your project to live and run the following commnad in a terminal:
```console
git clone https://github.com/afrouzi/tex_template tex_template  && cd "$_" && source ./initialize.bash
```
